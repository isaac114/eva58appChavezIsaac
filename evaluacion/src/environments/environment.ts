// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyAeWQtM56Lb4exObsbYltGyfv0dHeSBf_Q",
    authDomain: "eva58appchavezisaac.firebaseapp.com",
    projectId: "eva58appchavezisaac",
    storageBucket: "eva58appchavezisaac.appspot.com",
    messagingSenderId: "157989164934",
    appId: "1:157989164934:web:9cf60bd8590cf77b5dfe07"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
